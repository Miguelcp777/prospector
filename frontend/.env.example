# Estas dos son públicas por definición: viajan al navegador en el bundle.
# La clave publicable solo puede lo que permitan las políticas RLS.
#
# SUPABASE_SERVICE_ROLE_KEY no aparece aquí y no debe aparecer nunca: se
# salta la RLS entera. Si acaba en el frontend, cualquiera lee los leads de
# todos los clientes.
#
# Copia este archivo a .env.local (que está en .gitignore) para trabajar.

VITE_SUPABASE_URL=https://tpfjeumrvdbciktmaaii.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
